# Frontend

Stack: **Vite + React 18 + TypeScript**, **Tailwind CSS v4** (token di tema in `src/index.css` via `@theme`), **TanStack Query v5** (dati), **react-router v6** (routing), **@supabase/supabase-js** (solo autenticazione), **Axios** (chiamate al backend), **lucide-react** (icone SVG).

## Route

| Percorso | Pagina | Accesso |
|---|---|---|
| `/` | Landing pubblica (hero, funzionalità, come funziona, piani, FAQ) | pubblico (redirect a `/app/bandi` se loggato) |
| `/login` | Accesso | pubblico |
| `/registrati` | Registrazione (un solo passo, solo dati anagrafici; il piano si sceglie dopo, in Abbonamento) | pubblico |
| `/accetta-invito` | Atterraggio del link d'invito famiglia (set password + accettazione) | pubblico |
| `/recupera-password` | Richiesta del link di reset via email | pubblico |
| `/reimposta-password` | Atterraggio del link di reset (nuova password) | pubblico |
| `/conferma-email` | Atterraggio del link di conferma registrazione (con reinvio se scaduto) | pubblico |
| `/app/bandi` | Elenco bandi con filtri | autenticato |
| `/app/bandi/:slug` | Dettaglio bando | autenticato |
| `/app/salvati` | Bandi salvati (preferiti) | autenticato |
| `/app/calendario` | Calendario mensile: eventi personali, scadenze bandi e — per progettisti e admin — disponibilità e appuntamenti | autenticato |
| `/app/notifiche` | Centro alert: tutte le notifiche, con filtro per azienda per gli Advisor | autenticato |
| `/app/azienda` | Tutto sull'azienda: dati aziendali e dossier certificato | autenticato |
| `/app/aziende` | Portafoglio aziende dell'Advisor (crea/attiva/rimuovi) | autenticato (visibile se `max_aziende > 1`) |
| `/app/ai-check` | Cruscotto AI-check (quota del piano e storico per bando) | autenticato |
| `/app/preferenze` | Preferenze di filtro/notifica e avvisi email sui nuovi bandi | autenticato |
| `/app/abbonamento` | Piano attuale, cambio piano, sezione «Pagamento e rinnovo» e catalogo add-on | autenticato |
| `/app/fatturazione` | Dati di fatturazione (anagrafica a cui intestare le fatture) | autenticato |
| `/app/checkout` | Checkout di piano o add-on (`?piano=` / `?addon=`): riepilogo, dati fatturazione, widget Revolut | autenticato |
| `/app/checkout/esito/:purchaseId` | Esito del pagamento (polling + verifica manuale) | autenticato |
| `/app/acquisti` | Storico acquisti con stato | autenticato |
| `/app/profilo` | Profilo personale e gestione account collegati | autenticato |
| `/app/consulenze` | Richieste di consulto dell'Azienda (lista) | autenticato |
| `/app/consulenze/:id` | Dettaglio consulenza: proposte, accettazione, appuntamento | autenticato |
| `/app/progettista/richieste` | Pool richieste + consulenze assegnate | progettista o admin |
| `/app/progettista/richieste/:id` | Dettaglio richiesta: AI-check, proposta, dati full | progettista o admin |
| `/app/progettista/disponibilita` | Redirect a `/app/calendario` (la pagina dedicata non esiste più) | autenticato |
| `/app/admin/utenti` | Gestione utenti | solo admin |
| `/app/admin/piani` | Gestione piani di abbonamento | solo admin |
| `/app/admin/addon` | Gestione catalogo add-on | solo admin |

Guardie: `ProtectedRoute` (sessione Supabase), `AdminRoute` e `ProgettistaRoute` (ruolo dal profilo via `/me`) in `src/components/layout/guards.tsx`.

**Navigazione** (`AppShell.tsx`): l'header è organizzato per **famiglie di destinazioni**, così da essere leggibile per ogni ruolo e ogni piano.

- **Barra (a sinistra) = sola navigazione**: link diretti **Bandi · Salvati · Calendario · AI-check · Consulenze** (le Consulenze sono una feature a sé — aiuto umano sui bandi — e stanno in vista, non più sepolte in un menu). Per progettisti **e admin** (parità completa, `lib/roles.ts:hasAreaProgettista`) il menu **«Progettista»** (Richieste; senza icona — le disponibilità si gestiscono dal Calendario); per gli admin **«Admin»** (Utenti, Piani, Add-on, icona scudo). I menu a tendina usano `components/layout/NavMenu.tsx` (chiusura su selezione, click fuori ed Esc; trigger attivo quando la rotta è nel gruppo). La nav per esteso entra da `lg`; sotto, il menu hamburger elenca i link diretti + le sezioni di ruolo.
- **A destra = contesto e account**, tre elementi: **menu Azienda** (`components/layout/CompanyMenu.tsx`), **campanella** (`NotificationBell`), **menu Account** (`UserMenu`).
  - **Menu Azienda** unifica tutto ciò che riguarda l'azienda su cui operi: per i **non‑Advisor** è un **link diretto «Dati azienda»** (→ `/app/azienda`); per gli **Advisor** è un dropdown col nome dell'azienda attiva che contiene lo **switch** fra le aziende clienti + le scorciatoie **«Dati azienda»** (`/app/azienda`) e **«Gestisci aziende»** (`/app/aziende`, il portafoglio). Risolve la vecchia ambiguità **«Azienda» vs «Aziende»**: ora **«Dati azienda»** (i dati di *una* azienda) e **«Gestisci aziende»** (il portafoglio Advisor) sono nomi distinti; le rotte non cambiano.
  - **Menu Account** (l'avatar) contiene **solo** roba da account: link al **profilo** (nome + email), **Preferenze**, **Abbonamento**, **«Account collegati»** (deep-link alla `FamilyCard` in `/app/profilo#collegati`, visibile solo al **titolare** con posti collegati e **non Advisor** — mutua esclusività v1) ed **«Esci»**. Sparisce il vecchio menu «Impostazioni», che non conteneva impostazioni.
  - I due menu del cluster destro restano visibili a ogni breakpoint (su schermo stretto mostrano solo l'icona). Larghezze medie: il nome nell'avatar compare da `xl` e i link diretti hanno `whitespace-nowrap`, così l'header non va a capo nemmeno per un admin + progettista + Advisor.

**Landing pubblica** (`pages/Landing.tsx`): pagina marketing per l'utente sloggato (redirect a `/app/bandi` se c'è sessione). Sezioni: header sticky con ancore (Funzionalità · Come funziona · Piani · FAQ, smooth-scroll) + Accedi/Registrati; hero a due colonne (gradiente firma `from-brand-950 via-brand-900 to-brand-700`) con `HeroShowcase` (mock di prodotto *illustrativo* costruito dai token — scheda bando + widget AI-check con anello punteggio via `lib/scoreColor.ts`, `aria-hidden`); problema→soluzione; griglia funzionalità con l'**AI-check in evidenza** (`FeatureCard` variante `featured`); «come funziona» in 4 passi; «perché BandoFit» + fascia statistiche (nessuna statistica di utenti inventata; il **conteggio bandi è un claim di marketing**, non un dato: vive in `lib/copy.ts` ed è ripetuto in hero, FAQ e fascia — da lì, per non divergere); piani (riuso di `usePlans`+`PlanCard`, gestione `su_richiesta` invariata); FAQ (`Faq`, accordion `<details>` nativo); CTA finale; footer ricco (ancore, Accedi/Registrati, `PoweredBy`). Copy onesto: il tema scadenze punta sul Calendario reale; gli **alert email sui nuovi bandi sono attivi** e le card dei piani (riuso `PlanCard`) ne descrivono il ritardo reale. Componenti presentazionali co-locati in `components/landing/` (`HeroShowcase`, `SectionHeading`, `FeatureCard`, `Faq`), solo token, nessuna logica di rete.

## Flusso di autenticazione

- `src/lib/supabase.ts`: client del progetto **primario**, usato solo per `signInWithPassword` / sessione.
- La registrazione (`Register.tsx`, **un solo passo**, card `max-w-lg`) raccoglie anche **telefono** (obbligatorio, `lib/telefono.ts`: gemello del validatore backend — normalizza in E.164 con default +39, lo zero dei fissi si conserva; validazione on-submit con errore per campo) e **posizione aziendale** (obbligatoria, `Combobox` ricercabile con le voci di `useJobPositions`, endpoint pubblico; con «Altro» compare il campo facoltativo «Specifica la posizione»; se la lookup non carica: combobox disabilitato + errore inline con Riprova). Layout a coppie semantiche, impilate su mobile: Nome|Cognome, Email|Telefono, Azienda|Posizione. Il payload invia `telefono` già normalizzato e `job_position_slug` (mai l'id: è lo slug l'identificativo stabile) → il backend li mette nei metadata e il trigger DB crea profilo e abbonamento.
- **Il piano non si sceglie più alla registrazione** (non c'è più uno step 2 con la griglia piani): si parte da **Gratuito** e lo si attiva dopo dal checkout. Il `?piano=` della landing viaggia solo come **intento** nel payload di `POST /auth/register` (`plan_slug`, ripiegato su `gratuito` per i piani `su_richiesta`, che il backend rifiuterebbe con `400`) — finisce nello `user_metadata` del signup e la pagina Abbonamento lo rilegge da lì per il banner «Volevi il piano X». Se l'intento è un piano a pagamento, una nota sotto il submit lo dichiara («completerai l'acquisto dopo il primo accesso»). `Register.tsx` usa `usePlans` solo per qualificare quello slug, non per mostrare le card.
- **La registrazione non chiede la password**: si sceglie aprendo il link di conferma (`ConfermaEmail.tsx`), che applica password + conferma in un colpo — stesso schema di `AccettaInvito`. È il fulcro dell'anti-enumerazione lato API (vedi `docs/api.md`, `POST /auth/register`), non una preferenza di UX. La risposta è **sempre `202`**: il pannello di esito è quindi neutro («ti abbiamo scritto a…», modello `RecuperaPassword`) e non dice se l'account è stato creato — quella risposta sta nell'email. Accanto ha le vie d'uscita **Reinvia** (chiama `/auth/resend-confirmation`, neutro e con cooldown proprio), **Ho sbagliato indirizzo** e **Vai al login**: senza, chi sbaglia l'indirizzo resterebbe bloccato (il submit è disabilitato finché c'è un esito). `ConfermaEmail` senza token non è un errore ma il **form di richiesta** del link.
- **`PasswordStrengthMeter`** (`components/ui/`): indicatore **informativo** (non blocca mai il submit) sotto i campi password di `ConfermaEmail`, `ReimpostaPassword` e `AccettaInvito` — barra a 3 segmenti + etichetta testuale Debole/Media/Forte in un `role="status"` (mai solo colore). Motore **zxcvbn-ts caricato lazy** (`lib/passwordStrength.ts`: promise-singleton di dynamic import di `@zxcvbn-ts/core` + dizionari common/it) alla prima digitazione — i dizionari (~700 kB gz) restano fuori dal bundle iniziale delle pagine pubbliche; se il download fallisce il meter semplicemente non compare. `userInputs` penalizzano le password derivate dai propri dati.
- `src/lib/api.ts`: istanza Axios con interceptor che allega `Authorization: Bearer <token>`; su `401` fa `signOut` e riporta al login. `apiErrorMessage()` estrae il messaggio dal formato errori del backend (`ERR_NETWORK` e `ECONNABORTED` hanno il loro), `apiErrorCode()` il codice, per chi all'errore deve **reagire** e non solo mostrarlo. **Nessun timeout globale** (AI-check e import legittimamente durano minuti): lo imposta la singola chiamata che sa quanto può aspettare.

## Famiglia di account

- **Profilo** (`Profilo.tsx`): dati personali (con verifica CF), **telefono** con validazione E.164 (`lib/telefono.ts`) e **posizione aziendale** (`Combobox`, facoltativa e azzerabile; con «Altro» compare il campo «Specifica»; una posizione **disattivata** dopo la scelta resta visibile — l'opzione corrente viene aggiunta alle options se non è più nel catalogo attivo, ma una volta cambiata non è riselezionabile). Regola chiave del salvataggio: telefono/posizione/«Altro» **viaggiano nel PATCH solo se cambiati** — così i telefoni pre-0022 non in E.164 non bloccano il salvataggio degli altri campi. Rimandi compatti ad Azienda (`AziendaTeaser`), Preferenze (`PreferenzeTeaser`) e Abbonamento (`AbbonamentoTeaser` — piano attuale + link) e «Gestione account» (`FamilyCard`: contatore X di N, badge stato, azioni Reinvia/Riattiva/Rimuovi, dialog di invito).
- **Pagina «Abbonamento»** (`/app/abbonamento`, voce in navigazione): griglia `PlanCard` con cambio piano (il dialog di conferma avvisa se il downgrade retrocederà account; figlio attivo → card «Piano ereditato da …» senza switch). Dal modulo pagamenti il cambio verso un piano **a pagamento** passa dal checkout: il `409 payment_required` di `POST /me/subscription` viene intercettato e porta a `/app/checkout?piano=<slug>`; verso un piano gratuito da uno **pagato non scaduto** si programma la **disdetta alla scadenza** (`useScheduleDowngrade`, banner nello stato «Pagamento e rinnovo» — vedi la sezione Pagamenti). Sotto i piani, la sezione **Add-on**: catalogo gestito dagli admin (`useAddons`, key `["addons"]`, sezione nascosta se vuoto), card `AddonCard` (nome, descrizione, prezzo una tantum senza «/anno»). Il bottone «Acquista» degli add-on **a pagamento** porta al **checkout reale** (`/app/checkout?addon=<slug>`); per i `gratis` la CTA «Attiva» resta sul punto di estensione `purchaseAddon(slug)` in `lib/addons.ts` (stub: dialog «In arrivo»). Admin: `/app/admin/addon` (`AdminAddon.tsx`, clone di `AdminPiani`: card-editor, slug immutabile, «non si eliminano: si disattivano», hook `useAdminAddons`/`Create`/`Update` con doppia invalidazione).
- **Banner d'intento piano** in cima ad Abbonamento (R1): «Volevi il piano X — completa l'acquisto» con CTA al checkout, `useSessionDismissible("intento-piano")`. Compare solo se il piano desiderato in registrazione (letto dallo `user_metadata` del signup, `plan_slug`) esiste ancora, è a pagamento acquistabile e l'utente **non** è già su un piano pagato (a quel punto l'intento è soddisfatto e sparisce).
- **«I miei addon» — inventario per card** (migration 0028, `useMyAddons`): ogni `AddonCard` di un add-on **posseduto** (`quantita > 0`) monta `InventarioAddon` — badge **«Hai N …»** + **storico movimenti** a scomparsa (ultimi 20 dal ledger via `useMyAddonLedger`, caricato **on-demand** alla prima apertura; etichette italiane dei tipi in `MOVIMENTO_LABELS`, delta col segno). L'inventario è un **arricchimento**: se fallisce o è vuoto, il catalogo resta intatto. L'acquisto di un add-on a pagamento accredita ora un'**unità nell'inventario**, non più un credito riga-per-riga.
- **Modalità prezzo** (`tipo_prezzo` su piani e add-on): l'helper puro **`prezzoDisplay(tipo, etichetta, valore)` in `lib/prezzo.ts`** è l'unico punto che decide il testo del prezzo — `importo` → `formatPrezzo` (+ « /anno» sui piani), `gratis` → «Gratis», `su_richiesta` → `etichetta_prezzo` o fallback «Su richiesta». Con `su_richiesta` la CTA diventa «Richiedi una consulenza» (piani in `Abbonamento.tsx`, add-on in `AddonCard`) e passa dal **punto di estensione `requestConsultation({kind, slug})` in `lib/consulenza.ts`** — oggi stub gemello di `purchaseAddon`: apre il dialog «Richiesta in arrivo»; il blocco vero è nel backend (400 su cambio piano e registrazione). Add-on `gratis` → CTA «Attiva», stesso flusso `purchaseAddon`. In `Register.tsx` i piani su richiesta sono **visibili ma non selezionabili** (card non interattiva + nota; il fallback dello slug `?piano=` salta i piani su richiesta) e nella **Landing** pubblica la loro CTA diventa «Richiedi una consulenza» → `/registrati` senza deep-link `?piano=` (che verrebbe scartato). Nei form admin il select «Prezzo mostrato come» disabilita prezzo/etichetta secondo la modalità (pattern del campo «Giorni di preavviso») e il controllo di validità sul prezzo si applica solo in modalità «importo».
- **Inviti**: `InviteBanner` (in `AppShell`) mostra agli utenti esistenti l'invito con Accetta (avvisando che l'abbonamento attuale verrà annullato) / Rifiuta; `/accetta-invito` gestisce il link Supabase degli utenti nuovi — cattura l'hash **prima** che supabase-js lo consumi per riconoscere i link scaduti (`otp_expired`), poi form password e accettazione automatica.
- **Admin**: colonna Famiglia (badge Titolare/In famiglia/Invitato/Retrocesso + email del titolare), piano «(ereditato)» e select disabilitata per i figli.

## Pagamenti (checkout, acquisti, rinnovo)

- **Regola di fondo**: i dati carta vivono SOLO nel popup del provider — il frontend passa a `@revolut/checkout` il solo token dell'ordine, in modalità `REVOLUT_MODE` (`lib/revolut.ts`: `VITE_REVOLUT_MODE`, default `sandbox`, quella che NON muove denaro vero). Gli account **collegati attivi** vedono ovunque il pannello «Gestito dall'account titolare» (il backend risponde `403`).
- **Checkout** (`pages/Checkout.tsx`, `?piano=` o `?addon=`, hook `useCheckout.ts`): riepilogo dal **preventivo server** (`useCheckoutPreview` → listino, credito per il periodo residuo, IVA o riga «Reverse charge art. 7-ter» per i soggetti UE, totale, nuova scadenza) — il frontend non calcola nulla. Se l'anagrafica di fatturazione manca, il **`BillingProfileForm` compare inline** prima del pagamento (al salvataggio la preview si ricalcola: l'IVA dipende dal soggetto). Checkbox «Rinnova automaticamente alla scadenza» (default on, con la nota del preavviso ≥7 giorni; solo per i piani) — la scelta è **congelata nell'ordine creato**: si sblocca solo con un nuovo checkout. «Paga» crea l'ordine (`useStartCheckout`) e apre il **popup Revolut**; su annullo/declino il purchase resta `in_attesa` e «Riprova il pagamento» **riapre lo stesso ordine con lo stesso token** (niente nuovo POST). Un `409` (pagamento già in corso) linka a `/app/acquisti`.
- **Esito** (`pages/CheckoutEsito.tsx`): il webhook di norma arriva in secondi — polling del purchase ogni 2s per ~90s; oltre, bottone **«Verifica ora»** che chiama `POST /me/purchases/{id}/sync` (stessa strada del webhook, idempotente). A pagamento confermato invalida `["me"]` (piano/quote/scadenza possono essere cambiati); su esito fallito propone di riprovare con un nuovo checkout dello stesso oggetto.
- **Acquisti** (`pages/Acquisti.tsx`, `usePurchases`): storico paginato con badge di stato (`in_attesa`/`pagato`/`fallito`/`scaduto`/`annullato`/`gratuito`) e link all'esito per i pagamenti in corso. Lo storico include anche le righe **`kind='addon_admin'`** (accrediti add-on da amministratore, a 0 €, `gratuito`; migration 0028). La fattura non si scarica da qui: viene emessa fuori piattaforma dal titolare e recapitata a parte.
- **Banner upgrade globale** (`components/shared/UpgradeBanner.tsx`, in `AppShell` accanto a `InviteBanner`): per chi è su un piano **gratuito** e può salire, invito sobrio a vedere i piani (link ad Abbonamento). **Non** compare a chi **eredita** il piano (non può comprare), né su `/app/checkout` e `/app/abbonamento` (ridondante); «chiudi» vale per la sessione (`useSessionDismissible`). Il criterio «esiste un piano superiore acquistabile» è lo stesso `ordering` del `QuotaUpgradeBanner`, ristretto ai piani self-serve a pagamento.
- **Fatturazione** (`pages/Fatturazione.tsx`, `useBillingProfile`): l'anagrafica corrente editabile (`BillingProfileForm`, lo stesso componente del checkout inline); ogni fattura fotografa i dati validi al momento dell'acquisto.
- **Sezione «Pagamento e rinnovo»** in Abbonamento (`components/shared/SubscriptionManagement.tsx`, hook `useSubscriptionManagement.ts`): visibile solo con un piano a pagamento attivo (o un cambio programmato da mostrare). Contiene: **metodo salvato** (etichetta mascherata; «Aggiungi» apre il widget su un **ordine a 0 €** — il metodo compare via riconciliazione, con un breve polling di cortesia; «Rimuovi» con conferma spegne anche il rinnovo), **toggle rinnovo automatico** (il `409` per metodo mancante apre il flusso di aggiunta carta e a carta salvata il rinnovo si riattiva da solo), **disdetta** (downgrade programmato a Gratuito, con banner del cambio programmato e **«Annulla il cambio»**). Le voci **«Dati di fatturazione»** e **«I tuoi acquisti»** vivono nel menu Account (avatar).
- **Admin** (`/app/admin/pagamenti`, `pages/AdminPagamenti.tsx`): vista a tre tab — acquisti globali (filtri stato/tipo), **registro fatture in sola lettura** (le righe nuove restano «Da emettere»: l'emissione è fuori piattaforma; gli altri stati sono storici della trasmissione SDI), anomalie (incassi orfani da riconciliare, con banner rosso e «Segna come risolta»). Hook in `hooks/useAdmin.ts` (`useAdminPurchases`/`useAdminInvoices`/`useAdminAnomalies`/`useResolveAnomaly`). Il cambio piano da `AdminUtenti.tsx` richiede una **motivazione** (registrata nello storico con l'attore).

- **`pages/Azienda.tsx`** è l'unica casa dei dati aziendali, in due sezioni: **1) «Dati aziendali»** (`CompanyCard`): riepilogo in sola lettura dei campi compilati con bottoni «Importa da P.IVA» e «Modifica» — il form completo (Combobox con ricerca per ATECO/settori/regioni, `TagSelect` multi-valore per le **categorie di beneficiario**, sede, dimensione, contatti) si apre solo in modifica, con Salva/Annulla; i figli attivi vedono il solo riepilogo. Le categorie di beneficiario si **dichiarano** (PMI, Startup, Organismo di formazione, Ente pubblico…): dalla visura non sono deducibili, e finché sono vuote il requisito «beneficiari» del pre-check resta *non valutato*. Il form non viene mai risincronizzato durante la modifica (un refetch non cancella ciò che si sta scrivendo); senza alcun dato la card parte direttamente dal form. **2) «Dossier certificato»**: sezioni collassabili (`DossierSection`/`DossierRow` nascondono i campi vuoti) — Anagrafica, Attività e ATECO, Sede e unità locali, Persone e cariche (`PeopleTable`), Partecipazioni, Dipendenti, Dati economici, Contatti, Attributi — con badge stato impresa/«Startup innovativa»/«Dati di test», data di aggiornamento e bottone «Aggiorna»; empty state con CTA «Importa da P.IVA». Nel Profilo resta solo il rimando `AziendaTeaser`.
- **Import** (`ImportCompanyDialog`, aperto da `CompanyCard` e dalla sezione dossier): macchina a stati **form → attesa → anteprima → esito**, con la deviazione «annullare?» prima di buttare via un recupero già pagato. Il form ha P.IVA precompilata e **checksum locale** (`lib/partitaIva.ts`, port di `validate_partita_iva`): un refuso si scopre senza spendere, e «partita IVA non valida» non si confonde più con «non presente nel Registro Imprese» (404). L'anteprima (`usePreviewImport`) è di **sola lettura**: identità dell'azienda (con avviso se lo stato non è «Attiva»), campi che verranno compilati e campi già compilati che **non** verranno toccati; solo «Conferma e importa» (`useConfirmImport`) scrive. L'esito mostra i campi compilati e i chip «ATECO secondari» da aggiungere alle preferenze con un click.
  Durante le chiamate il dialog è **`dismissible={false}`**: era il vero bug dell'import. Chiudendolo con Esc o col click sul backdrop la richiesta proseguiva, i dati venivano importati **e pagati**, ma l'esito viveva nello stato locale del dialog e spariva con lui. Stesso motivo sulla domanda «annullare?», dove una X sceglierebbe «sì» al posto dell'utente. L'anteprima, invece, si può chiudere: il draft resta valido 30 minuti lato server e riaprirla non costa nulla.
- **Hook**: `useCompanyDossier` (query key `["company-dossier"]`), `usePreviewImport` (nessuna cache da toccare: l'anteprima non scrive) e `useConfirmImport` (aggiorna anche `["company"]`); `usePreferences`/`useSavePreferences` (key `["preferences"]`).

## AI-check

- **Tono costruttivo, mai bocciature secche**: il report è generato da un modello e può sbagliare — gli esiti in UI sono «In linea col bando» (emerald) e «Dati da completare» (amber); per l'esito negativo NESSUN badge — parlano il colore del punteggio e i verdetti dei singoli requisiti. Il punteggio non viene mai "svalutato" o nascosto.
- **Mai nomi tecnici davanti all'utente**: i campi citati dall'AI (`derived.ateco_secondari[1]`, `settore_nome`…) passano da `lib/aiCheckFields.ts` → etichette italiane con fonte («ATECO secondari (Registro Imprese)»); il prompt di matching impone linguaggio naturale nelle motivazioni. I dati mancanti guidano con due link: completa i dati (Profilo) o importa dal Registro Imprese (Azienda).
- **`AiCheckCard`** (prima card della sidebar in `BandoDetail.tsx`): quota residua del piano, CTA «Verifica compatibilità» con dialog di conferma (consumo quota + durata 1-2 minuti), stato «Analisi in corso…» con polling ogni 4s (`useAiChecksForBando`, key `["ai-check", slug]` — stesso pattern dei documenti: il polling si spegne per le pending oltre 12 minuti, il backend le chiude a 10), esito sintetico + punteggio con ancora al report. I figli attivi vedono i risultati ma non avviano.
- **`AiCheckReport`** (sezione full-width `#ai-check-report` sotto la scheda del bando): esito + punteggio a colori (rosso→verde per fasce, numero e barra, lib/scoreColor.ts) + badge «Stima del punteggio ufficiale»/«Punteggio euristico interno», selettore delle versioni (storico, con data e ora), righe espandibili (`<details>`) per ogni requisito/criterio con **citazione del bando** (sezione + testo, flag «citazione non verificata») e **dato aziendale usato** in linguaggio naturale, punti di forza/debolezza, callout ambra dei dati mancanti, disclaimer.
- **Pagina «AI-check»** (`/app/ai-check`, voce in navigazione): cruscotto con la quota del piano (disponibili quest'anno, con barra di consumo) e storico **raggruppato per bando** — l'analisi più recente in evidenza con badge, mini-barra punteggio, numero versioni e bottone «Apri report» che porta al report sul dettaglio bando (`useAiChecks`, key `["ai-checks"]`, polling 10s se c'è un'analisi in corso).
- **`QuotaUpgradeBanner`** (`components/aicheck/`, sotto la quota): callout **inline, non modale e non bloccante** quando la quota del piano è consumata per **almeno due terzi**. La soglia si deriva dal piano (`quota.usati * 3 >= quota.totale * 2`, aritmetica intera: in virgola mobile `2/3` cade esattamente sul bordo), mai da un numero fisso. A **quota zero** il messaggio cambia (rosso, «AI-check esauriti») ma **la CTA resta**: è l'unica via d'uscita, e a bloccare le analisi è già il backend. Niente banner se il piano non include AI-check (lo dice già lo `StatTile`). Niente CTA per un **figlio attivo** (non può cambiare piano) né per chi è già sul piano con `ordering` più alto — «piano massimo» non è modellato a DB e si deriva da lì. Un piano superiore `su_richiesta` resta un upgrade valido: la CTA porta ad Abbonamento, che sa già diventare «Richiedi una consulenza». Chiudibile: la scelta persiste in `localStorage` via **`useDismissible`** con chiave `bandofit:aicheck-quota:{periodo_inizio}:{livello}` — chiudere l'avviso di esaurimento imminente **non** zittisce quello di quota finita, e un nuovo periodo di abbonamento (o un cambio piano, che resetta `data_inizio`) lo ripropone. `localStorage` inaccessibile → l'avviso non ricorda la chiusura, e basta.
- Le stringhe user-facing **ripetute in più punti** (claim bandi della landing, copy del banner quota, copy dell'import P.IVA) stanno in **`lib/copy.ts`**. Non è i18n: è l'antidoto alla divergenza. Le stringhe usate una volta sola restano inline nel componente, come nel resto dell'app.
- Hook in `hooks/useAiCheck.ts`; `useRequestAiCheck` invalida `["ai-check", slug]` e `["ai-checks"]`.

## Punteggio di compatibilità (pre-check)

- La frazione è **requisiti soddisfatti / requisiti valutabili** (es. `3/4`): dentro un requisito le voci sono alternative, una in comune basta. Vedi `docs/api.md`.
- **`CompatibilitaBadge`** (`components/bandi/`): pill **«Compatibilità 3/4»** — etichetta esplicita + frazione colorata per banda con la stessa scala dell'AI-check (`lib/scoreColor.ts`); percentuale ed esito per requisito nel tooltip/`aria-label`. Presente sulla riga badge di `BandoCard` e nell'header di `BandoDetail`, **subito e senza azioni** dell'utente.
- **`CompatibilitaCard`** (`components/bandi/`, in cima alla colonna laterale del dettaglio, sopra l'AI-check): frazione + barra colorata e, per ogni requisito (Regioni, Codici ATECO, Settori, Beneficiari), le voci del bando con **in comune evidenziate in emerald** (`matched_ids`) e un'icona di esito (`CircleCheck` / `CircleX` / `CircleDashed` per «non valutato», quando l'azienda non ha quel dato). Le voci in comune sono ordinate per prime; oltre `MAX_CHIP = 6` le altre si riassumono in un «+N» col `title` completo, perché in 320 px una lista lunga sfonda la colonna. Un bando **nazionale** non elenca venti regioni: mostra le sedi in comune e la nota «aperto a tutte le regioni». Senza P.IVA importata la card resta (le voci del bando sono comunque utili: è la vecchia **«A chi si rivolge»**, che assorbe) con un callout ambra e link ad Azienda; in sidebar restano poi solo le Tematiche.
- La **card dell'elenco** non mostra più la modalità di erogazione (Fondo perduto, Contributo in conto interessi…): affollava la riga senza aiutare a scegliere. Resta nell'header del dettaglio. La riga badge ha `pr-10`: il toggle «salva» è un overlay opaco in alto a destra e coprirebbe l'ultimo badge.
- Il valore arriva già calcolato dal backend nel campo `compatibilita` di `BandoListItem`/`BandoDetail` (tipi `Compatibilita`/`CompatibilitaDimensione` in `types/index.ts`): il frontend non ricalcola nulla e **rende badge e punteggio solo se il campo è presente** (profilo aziendale sufficiente, cioè P.IVA importata). Nessun nuovo hook o round-trip: viaggia con la lista e col dettaglio.

## Bandi salvati e Calendario

- **Salvataggio** (`SaveBandoButton`): toggle segnalibro con stato **ottimista sul Set degli id** (`useSavedIds`, key `["saved-bandi","ids"]`; rollback su errore, poi invalidazione della lista). Sulle card della lista il bottone è un **fratello sovrapposto** al link (`SavableBandoCard`: wrapper `relative` + bottone `absolute` — mai annidato nel `<Link>`, vedi Pattern chiave); sul dettaglio bando è un bottone inline accanto ad «Aggiungi scadenza al calendario» (visibile solo con una scadenza).
- **Pagina «Salvati»** (`/app/salvati`): griglia di `SavableBandoCard` con azione «Aggiungi scadenza al calendario» sotto ogni card (o link «Nel calendario» se già presente); i bandi **spariti dal catalogo** restano visibili come card tratteggiata non-cliccabile costruita dallo snapshot (badge «Non più disponibile», data di salvataggio, bottone Rimuovi). Paginazione a 20, hook `useSavedBandi(page)` (key `["saved-bandi", page]`).
- **Pagina «Calendario»** (`/app/calendario`, mese nell'URL `?m=YYYY-MM`): un'unica Card a tutta larghezza con **toolbar integrata** (mese + frecce + «Oggi»; legenda nell'intestazione di pagina) e griglia mensile nativa compatta in verticale (lunedì per primo, 6 settimane fisse, bordi interni sottili, numero in alto a sinistra, hint «+» al passaggio del mouse, `Intl it-IT` — nessuna libreria di date). **Niente pannello agenda**: click su un giorno → si apre subito il form di creazione (data precompilata); click su un chip evento → dialog di modifica; «+N altri» (celle affollate) e il tap su un giorno con eventi su mobile aprono il **dialog-elenco del giorno** (`DayEventsDialog`, righe → modifica, footer «Aggiungi evento»). Niente interattivi annidati: la cella è un div con un bottone di sfondo (`absolute inset-0`) e i chip come bottoni FRATELLI sovrapposti (`z-10`); su mobile i chip sono pallini presentazionali. `EventDialog`: titolo, data (`type="date"`), «Tutto il giorno», orari (`type="time"`), note (`TextareaField`, nuovo componente ui); per gli eventi bando la data è una riga fissa («deriva dal bando ufficiale») con link al bando; eliminazione con conferma a due passi. Hook `useCalendarEvents(anno, mese)` (key `["calendar", anno, mese]`); le mutazioni invalidano il prefisso `["calendar"]`; `useAddBandoDeadline` invalida anche `["saved-bandi"]`. Per i **progettisti** il calendario è anche il gestionale delle disponibilità (vedi «Consulenze e area progettista»): la griglia lavora su **item misti** (`CalendarItem` in `components/calendar/items.ts` — evento | slot | appuntamento, con helper per giorno/ordinamento/chip); per gli altri utenti nulla cambia (query progettista disattivate via `enabled`, classi chip evento invariate).

## Preferenze e «Bandi per te»

- **Pagina «Preferenze»** (`/app/preferenze`, voce in navigazione; nel Profilo resta un rimando compatto `PreferenzeTeaser`): layout a due colonne — a sinistra il riquadro fisso **«La tua azienda»** con i valori **ereditati** dai dati aziendali (ATECO, settore, regione e categorie di beneficiario dichiarate) come chip bloccate con icona edificio (sempre inclusi in «Bandi per te», si modificano dai dati aziendali) + contatore dei valori seguiti; a destra una card per ciascuna delle 7 faccette con chip ereditate/rimovibili e **`TagSelect`** (nuovo componente ui: multi-selezione con ricerca in stile Combobox, tendina che resta aperta, valori ereditati marcati e non selezionabili). **Barra di salvataggio fissa** in basso che compare solo con modifiche non salvate (Annulla/Salva), più link «Vedi i bandi per te» che apre la lista con il preset applicato. In testa alla colonna destra la card **«Avvisi email sui nuovi bandi»** (`AlertEmailCard`, hook `useAlertSettings`): toggle se il piano li include (col ritardo descritto nel copy), altrimenti nota upsell con link ad Abbonamento — stessa riga di verità del link di disiscrizione presente in ogni email. `PlanCard` descrive la feature col nuovo campo (`alert_ritardo_giorni`: «il giorno dopo la pubblicazione» / «dopo N giorni»); in AdminPiani il campo «Ritardo avvisi nuovi bandi (giorni)» (vuoto = esclusi, 0 = stesso giorno) affianca il vecchio «Preavviso scadenze», rimasto per la funzione futura.
- **`BandiPerTeButton`** (toolbar di BandiList): preset che applica ai filtri URL l'**unione** dei valori reali dell'azienda e delle preferenze personali (helper condiviso `lib/bandiPreset.ts`); evidenziato quando i filtri correnti coincidono, secondo click = rimozione del preset. Visibile solo se c'è almeno un valore.
  I valori dell'azienda vengono dai **facet** (`useCompanyFacets` → `GET /me/company/facets`), non dai campi del form: **tutte le sedi** e tutte le **divisioni ATECO**, non la sola sede legale e l'ATECO principale. `useCompany` resta la fonte del form, dove una regione e un ATECO sono per costruzione uno solo. Stessa fonte usa il riquadro «La tua azienda» in Preferenze. La cache `["company-facets"]` viene invalidata dal salvataggio dei dati aziendali e dalla conferma dell'import (i facet li ricalcola il server: non si ricostruiscono dalla risposta).

## Consulenze e area progettista

- **CTA «Consulto esperto»** (`components/bandi/ConsultoCard.tsx`, sotto `AiCheckCard` nella sidebar del bando): compare dopo **ogni** AI-check completato, solo se l'addon `consulto-esperto` è a catalogo (slug in `lib/consulenza.ts`, `CONSULTO_ADDON_SLUG`, che deve coincidere col backend). Il dialog di conferma include il **testo di consenso** (`CONSULENZE_COPY.consenso` in `lib/copy.ts`: elenca esattamente ciò che i progettisti vedranno — è parte della base giuridica, non riformularlo senza rivedere l'informativa). Solo il **titolare** attiva (`editable` dalla risposta AI-check); con una richiesta già aperta o assegnata sullo stesso bando la card rimanda alla consulenza esistente. **Gating a pagamento (migration 0028)**: se l'addon è **consumabile a pagamento** e l'inventario dell'utente è a zero (`useMyAddons`), il bottone «Richiedi il consulto» lascia il posto a **«Acquista una consulenza»** → `/app/checkout?addon=`; con un'unità in inventario (o addon `gratis`) il flusso è quello di prima. Finché l'inventario carica il bottone resta **disabilitato** (niente sfarfallio dello stato «bloccato»). Rete di sicurezza: se il backend risponde `payment_required` alla creazione (saldo cambiato sotto i piedi), la card ricade sullo stato «acquista».
- **Lato cliente** (`pages/Consulenze.tsx` + `ConsulenzaDetail.tsx`, hook `useConsulenze.ts`): lista con stato/proposte da valutare/appuntamento; nel dettaglio le proposte mostrano l'autore per **nome e cognome** (`nome_progettista`; il codice PRG resta nel payload per usi interni ma la UI del cliente non lo mostra — scelta «più umana»), con Accetta → **`SlotPicker`** (`components/consulenze/`: slot liberi raggruppati per giorno, selezione opzionale «accetta anche senza appuntamento») e Rifiuta; dopo l'assegnazione: prenota/annulla appuntamento; finché è `nuova`: annulla richiesta (dialog di conferma). Le azioni compaiono solo con `editable` (gli account collegati leggono). Con un appuntamento confermato il box mostra **«Avvia videochiamata»** (stanza Jitsi in nuova scheda, `noopener noreferrer`) + «Copia link» — componente condiviso `components/consulenze/VideocallButton.tsx`, sempre attivo e NON gated su `editable` (aprire/copiare non è una mutazione); il bottone compare anche nell'`AppuntamentoDialog` del calendario progettista e nel box appuntamento di `RichiestaDetail`. Le mutation ritornano il dettaglio aggiornato → `setQueryData(["consulenze", id])` + invalidazione della lista.
- **Lato progettista** (`pages/progettista/Richieste.tsx` + `RichiestaDetail.tsx`, hook `useProgettistaRichieste.ts`): pool con i dati parziali (ragione sociale, P.IVA, denominazione, email, esito+punteggio) e sezione «Assegnate a te»; nel dettaglio il **report AI-check completo** via **`AiReportBody`** (`components/bandi/` — corpo presentazionale estratto da `AiCheckReport`, con `mostraAzioni={false}`: i link «completa i dati» sono del titolare) + form proposta (una sola aperta; ritiro possibile, poi se ne può inviare un'altra). I **dati full** (`DossierCompleto`) si caricano **su azione esplicita** («Apri i dati completi»): ogni lettura è registrata lato server, niente fetch eager. Il dossier è **`DossierView`** (`components/company/dossier/` — estratto da `Azienda.tsx`, che ora lo riusa).
- **Disponibilità nel Calendario** (niente pagina dedicata: `/app/progettista/disponibilita` è un redirect): per chi ha l'area progettista (progettisti e admin) la pagina Calendario mostra e gestisce anche **slot liberi** (chip emerald «HH:MM · Disponibile») e **appuntamenti** (chip violet con la ragione sociale), con legenda a 4 voci. **Convenzione mista deliberata** (documentata in `components/calendar/items.ts`): gli eventi sono wall-clock italiano, slot/appuntamenti sono **istanti UTC** ancorati al giorno LOCALE del browser (`formatSlotGiorno`/`formatSlotOra`). **Dedup**: uno slot prenotato non si renderizza — il suo booking appare già come appuntamento con gli stessi orari (annullando l'appuntamento la chip Disponibilità ricompare). Le disponibilità passate non compaiono (l'endpoint filtra `fine >= now`); gli appuntamenti passati restano visibili.
- **Gestione**: il click su un giorno apre per i progettisti il chooser `AddItemChooser` (Evento personale / Slot di disponibilità). `SlotDialog` crea (data dal giorno cliccato, orari default 10:00–10:30, conversione in UTC con `toISOString`) con **Ripetizione** opzionale (Non si ripete / Ogni giorno / Ogni giorno feriale lun–ven / Ogni settimana / Ogni mese + «Ripeti fino al», max 12 mesi): l'espansione in occorrenze è in `lib/ricorrenza.ts` (ogni istante nasce come `Date` locale → l'orario a muro resta stabile attraverso i cambi di ora legale; il mensile salta i mesi privi del giorno; cap 370 occorrenze) e viaggia su `POST /progettista/slots/serie` (`useCreateSlotSerie`) — gli slot sovrapposti a disponibilità esistenti vengono **saltati** e il dialog mostra l'esito con i conteggi. In modifica: salvataggio via `useUpdateSlot`, «Elimina» a due passi, e «Elimina la serie» (`useDeleteSlotSerie`) che rimuove i soli slot liberi riportando i mantenuti-perché-prenotati. `AppuntamentoDialog` (click su una chip appuntamento): azienda, bando, email, link «Vedi la consulenza» e annullo a due passi (`useAnnullaAppuntamento`, che invalida anche gli slot).
- **Notifiche** (`components/layout/NotificationBell.tsx`, hook `useNotifications.ts`, key `["notifications"]`, polling 60s): badge con le non lette (9+ oltre), pannello a tendina col pattern di `NavMenu`; click su una voce → segna letta + naviga al deep-link `url`; «Segna tutte come lette». I contenuti sono minimizzati: i dettagli si leggono alla destinazione, dove vale l'autorizzazione del server.
- **AdminUtenti**: il cambio ruolo è un **select a tre valori** (Cliente/Progettista/Admin, etichette da `RUOLO_LABELS` in `lib/copy.ts`) con dialog di conferma che spiega la transizione (promozione: codice + area progettista; demozione: il codice resta riservato); badge ruolo ternario col codice `PRG-…` sotto, filtro per ruolo esteso. **«Assegna addon»** (R5, `useAdminGrantAddon`) apre un dialog di grant: scelta dell'add-on dal catalogo (`useAddons`), quantità (**bloccata a 1 per i permanenti**), motivazione **obbligatoria**, più il riepilogo di ciò che l'utente già possiede (`useAdminUserAddons`). L'accredito è gratuito, a 0 €, registrato nello storico dell'utente con l'admin come attore.

## Pattern chiave

- **Filtri nell'URL** (`src/hooks/useBandiFilters.ts`): tutti i filtri della lista bandi vivono nei searchParams (csv per le faccette). L'URL è condivisibile, il back del browser funziona, e i parametri sono la query key di TanStack Query. Ogni modifica ai filtri riporta a pagina 1; la ricerca testuale ha debounce di 400 ms. Ordinamento di default «Più recenti» (`pubblicazione_desc`, omesso dall'URL); i bandi chiusi vengono sempre in coda (lato backend).
- **Faccette M:N**: `FacetGroup` collassabile con contatore, ricerca interna per le liste lunghe (90 settori, 89 ATECO); OR dentro la faccetta, AND tra faccette (implementato dal backend).
- **Contenuto ricco** (`ContenutoRenderer`): il campo `contenuto` del bando è JSON strutturato (sections → segments) e viene mappato a elementi React puri — mai `dangerouslySetInnerHTML`.
- **Stati ovunque**: ogni vista dati ha skeleton (caricamento), empty state con azione di reset ed error state con retry.
- **Conferme**: le azioni con effetto (cambio piano, sospensione utente, cambio ruolo) passano da `Dialog` (elemento `<dialog>` nativo: focus trap ed Esc inclusi). La prop **`dismissible={false}`** disattiva Esc, click sul backdrop e X quando chiudere farebbe danno — p.es. mentre è in volo una chiamata a pagamento di cui si perderebbe l'esito.
- **Submit dei dialog con form**: il bottone di conferma nel footer di un `Dialog` fa partire il submit con un **`onClick` esplicito** (`type="button"` + `handleSubmit` chiamato a mano), **mai** con l'associazione `form="id"` cross-boundary. Su Safari quell'attributo, dentro un `<dialog>` modale, fallisce e il browser valida invece i campi `required` **vuoti e non focalizzabili** di un eventuale form di sfondo (es. la scheda azienda in modifica dietro `ImportCompanyDialog`), bloccando il submit in silenzio. Per lo stesso motivo il `<form>` di questi dialog porta **`noValidate`**: la validazione è quella applicativa in `handleSubmit`, non quella nativa del browser. Vale per `ImportCompanyDialog`, `EventDialog`, `SlotDialog`.
- **Azioni sopra le card-link**: mai un bottone DENTRO un `<Link>` (interattivi annidati vietati) — il pattern è un wrapper `relative` con la card-link e il bottone come **fratello** `absolute` (es. `SavableBandoCard`); nelle celle del calendario, un solo bottone per cella e contenuti presentazionali.

## Brand assets

- Loghi in `src/assets/` (PNG trasparenti, importati da Vite): `logo-orizzontale.png` (unico lockup fornito: topbar, footer **e** pagine auth) e `logo-icona.png` (sola icona documento+check, **ritagliata dall'orizzontale**); favicon e apple-touch-icon in `public/` sono la stessa icona ritagliata (512²/180²). Non esiste più un `logo-verticale.png` dedicato.
- Componente `Logo` (`components/layout/Logo.tsx`) con varianti `horizontal` (default) / `vertical` / `icon`. Il brand è un **singolo lockup orizzontale**: la variante `vertical` riusa la stessa immagine (più piccola) impilando sotto l'attribuzione. **Regola di brand: il logo BandoFit porta sempre con sé l'attribuzione "powered by EduNews24"** — accanto (con separatore) nella variante orizzontale, sotto nella verticale; solo la variante `icon` (favicon) ne è priva. L'attribuzione nel lockup non è cliccabile (il Logo è spesso dentro un `<Link>`); il link a https://edunews24.it vive nel componente `PoweredBy` (`components/shared/PoweredBy.tsx`), usato nel footer dell'app.
- Gli screenshot sorgente dei loghi restano solo locali (`Screenshot *.png` in .gitignore).

## Design system

- **Colore primario**: navy `#2C56C9` (scala `brand-50`→`brand-950` ancorata al logo — `brand-900 #182549` ≈ navy del wordmark; hover `brand-600 #2246A7`, tint `brand-50 #F3F5F9`); neutri slate; sfondo app `#F7F9FC` (`bg-surface`). Tutta la UI passa dai token `brand-*`: la palette si ritara dagli 11 `--color-brand-*` in `index.css` senza toccare i componenti. Il verde `emerald` (stati di successo) è volutamente vicino al teal del logo.
- **Semantici**: aperto = smeraldo, chiuso = slate, in apertura = ambra; scadenza ≤ 7 giorni = rosso, ≤ 30 = ambra.
- **Tipografia**: Sora (titoli, 600/700) + Inter (testo) via Fontsource (self-hosted); cifre tabellari (classe `.tabular`) per importi e date.
- **Superfici**: card `rounded-xl` con ombre morbide (`shadow-card`, `shadow-card-hover`); topbar sticky bianca con bordo.
- **Localizzazione**: tutta l'interfaccia è in italiano (dare del tu); importi `Intl.NumberFormat('it-IT')`, date `it-IT`.
- **Accessibilità**: focus ring visibili (`focus-visible:outline-brand-500`), label su ogni input, `aria-label` sulle icone interattive, `prefers-reduced-motion` rispettato, contrasti AA.

## Variabili d'ambiente

`VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY` (progetto **primario**, mai il secondario) + `VITE_API_BASE_URL` (default `http://localhost:8000/api/v1`) + `VITE_REVOLUT_MODE` (modalità del widget di pagamento: `prod` in produzione; default `sandbox`, il valore sicuro che non muove denaro vero — `lib/revolut.ts`).
